spring:
  datasource:
    url : jdbc:mysql:192.168.1.3:3306/board
    password: 3106
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
  hibernate:
    ddl-auto: create
  properties:
    hibernate:
      format_sql: true
      use_sql_comments: true
      show_sql: true
  database-platform: org.hibernate.dialect.MySQLDialect

  spring:
    jpa:
      hibernate.ddl-auto = update